<Page x:Class="Frontend.View.RootPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Frontend.View"
      mc:Ignorable="d"
      d:DesignHeight="1080" d:DesignWidth="1920"
      Title="Root"
      Height="{Binding WindowSettings.Height,Mode=TwoWay}"
        Width="{Binding WindowSettings.Width, Mode=TwoWay}">
    <!--RootPage Grid-->
    <Grid x:Name="MainGrid" Width="{Binding WindowSettings.Width, Mode=TwoWay}" Height="{Binding WindowSettings.Height,Mode=TwoWay}" Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition MinWidth="450" Width="1640*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="1000*" />
        </Grid.RowDefinitions>
 
        <!--Topbar-->
        <Border Grid.Column="0" Grid.ColumnSpan="2" BorderBrush="Black" BorderThickness="0,0,0,1"/>
        <Button Click="Home_Button_Click" Grid.Column="0" Style="{StaticResource LogoButton}"/>
        <Button Click="Logout_Button_Click" Grid.Column="1" Style="{StaticResource LogoutButton}" />

        <!--Sidebar-->
        <Border Grid.Row="1" Grid.RowSpan="2" BorderBrush="Black" BorderThickness="0,0,1,0"/>
        <Grid x:Name="LeftMenuGrid" Width="280" Height="1000" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="940" />
            </Grid.RowDefinitions>
            <Border BorderThickness="0,0,0,1" BorderBrush="Black" Grid.Row="0"/>
            <StackPanel Orientation="Vertical" Grid.Row="1">
                <Button Tag="Home" Click="Home_Button_Click" Style="{StaticResource MenuButton}" />
                <Button Tag="Mein Stundenplan" Click="Timetable_Button_Click" Style="{StaticResource MenuButton}"/>
                <Button Tag="Tauschboerse" Click="SharingService_Button_Click" Style="{StaticResource MenuButton}"/>
                <Button Tag="Stammdaten" Click="PersonalData_Button_Click" Style="{StaticResource MenuButton}"/>
                <Button Tag="Admin" Click="Admin_Button_Click" Style="{StaticResource MenuButton}"/>
            </StackPanel>
        </Grid>
        
        <!--MainFrame-->
        <Frame x:Name="MainFrame" Source="HomePage.xaml" Grid.Row="1" Grid.Column="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Page>
